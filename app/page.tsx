// Orbit v2 — root page.
//
// The narrative experience: a single scrolling page with a live CO₂
// counter, Carbon Almanac facts, inline quiz, and personalized signal
// reveal. Entirely client-rendered — no server data needed.

import NarrativePage from "@/components/orbit/NarrativePage";

export default function Page() {
  return <NarrativePage />;
}
